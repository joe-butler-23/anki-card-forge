{
  "type": "object",
  "additionalProperties": false,
  "required": ["cards"],
  "properties": {
    "cards": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["cardType", "front", "back"],
        "properties": {
          "cardType": {
            "type": "string",
            "enum": ["Basic", "Basic (type in the answer)"]
          },
          "front": {
            "type": "string",
            "minLength": 1
          },
          "back": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    }
  }
}
